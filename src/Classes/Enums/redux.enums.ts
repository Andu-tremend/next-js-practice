export enum SearchReducer {
  search = "search"
}

export enum FilterReducer {
  filter = "filter"
}

export enum RestaurantName {
  name = "restaurant"
}

export enum DataAction {
  start = "GET_DATA_START",
  success = "GET_DATA_SUCCESS",
  err = "GET_DATA_ERROR"
}